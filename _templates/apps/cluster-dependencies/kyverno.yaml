apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kf-dep--kyverno
  namespace: {{< .Values.argocd.namespace | quote >}}
  labels:
    app.kubernetes.io/name: kyverno
    app.kubernetes.io/component: cluster-dependencies
    app.kubernetes.io/part-of: deploykf
spec:
  project: {{< .Values.argocd.project | quote >}}
  source:
    repoURL: {{< .Values.repo.url | quote >}}
    targetRevision: {{< .Values.repo.revision | quote >}}
    path: {{< path.Join .Values.repo.generatorOutputPath "src/cluster-dependencies/kyverno" | quote >}}
  destination:
    server: {{< .Values.argocd.destination.server | quote >}}
    namespace: {{< .Values.cluster_dependencies.kyverno.namespace | quote >}}
  syncPolicy:
    syncOptions:
      ## kyverno will fail to sync if `Replace` or `ServerSideApply` is not true
      ## https://kyverno.io/docs/installation/#notes-for-argocd-users
      - ServerSideApply=true