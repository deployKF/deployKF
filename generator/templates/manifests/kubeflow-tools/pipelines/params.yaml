## allows Kustomize to replace `vars` in the following places
varReference:
  ## allow in Deployment (for setting default image tag)
  - path: spec/template/spec/containers/image
    kind: Deployment
